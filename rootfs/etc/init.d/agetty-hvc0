#!/sbin/openrc-run

name="agetty on hvc0"
supervisor=supervise-daemon
command="/sbin/agetty"
command_args_foreground="--autologin user42 -s hvc0 115200 xterm"
pidfile="/run/${RC_SVCNAME}.pid"
respawn_period=60

depend() {
	need mount-host9p
	after mount-host9p agetty-ttyS0
}
